/**
 * Minimal locale strings for the client slots. A real deployment can extend
 * this to zh-CN / ja / etc.; the keys map to the `locale` option passed to
 * `ctx.slots.register`.
 */
export const locale = {
  "costGovernor.section": "Usage & Cost",
  "costGovernor.section.zh": "用量与成本",
};

export const localeZh = {
  "costGovernor.section": "用量与成本",
};
